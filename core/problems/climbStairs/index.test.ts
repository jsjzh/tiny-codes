import { code1, code2, code3 } from '.';

const num = 20;
const sum = 10946;

it('code1', () => expect(code1(num)).toBe(sum));

it('code2', () => expect(code2(num)).toBe(sum));

it('code3', () => expect(code3(num)).toBe(sum));
